# App

Punto de entrada de composicion de la aplicacion.

Archivos principales:

- `AppRoot.tsx`: ensamblado raiz de providers y navegacion.
- `AppProviders.tsx`: providers globales (safe area + navigation).
